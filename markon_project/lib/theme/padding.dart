import 'package:flutter/material.dart';

const TrustPostOutterMargin =
    const EdgeInsets.only(left: 17, right: 17, top: 16);

const TrustPostMargin = const EdgeInsets.only(left: 17, right: 17, top: 16);

const TrustPostMarginHorizontal = const EdgeInsets.only(left: 17, right: 17);

const TrustPostMarginBtn = const EdgeInsets.only(top: 12, bottom: 12);

const TrustPostDashboardPadding = const EdgeInsets.only(top: 16, bottom: 16);

const TrustPostOutterPadding =
    const EdgeInsets.only(top: 16, right: 24, left: 24);

const TrustPostInnerPadding =
    const EdgeInsets.only(top: 16, right: 12, left: 12, bottom: 16);

const TrustPostSearchFieldPadding =
    const EdgeInsets.only(top: 0, right: 17, left: 17);

const TrustPostParagraph = const EdgeInsets.only(left: 8);

const TrustPostBoxedParagraph =
    const EdgeInsets.only(left: 8, top: 16, bottom: 16);

const TrustPostDialogPadding =
    const EdgeInsets.only(top: 30, right: 24, left: 24, bottom: 20);
